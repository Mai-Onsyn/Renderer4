import Application;
import Time;
import Types;

int main() {
    const int width = 800;
    const int height = 600;

    Application app("DisplayWindow", width, height);
    app.run();

    return 0;
}